import { ApplyFilterPipe } from './apply-filter.pipe';

describe('ApplyFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ApplyFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
